# Sanity CMS 셋업 가이드

이 사이트는 **Sanity** 라는 헤드리스 CMS로 컨텐츠를 관리합니다.
사장님이 직접 프로젝트·소개글·연락처를 추가/수정할 수 있습니다.

---

## 1. Sanity 프로젝트 생성 (1회만)

1. https://www.sanity.io 접속 → **Get started** 클릭
2. Google/GitHub 계정으로 가입 (무료 플랜 충분)
3. 가입 후 자동으로 첫 프로젝트가 생성되거나, 새 프로젝트 만들기 버튼 클릭
4. 프로젝트 이름은 자유 (예: `soc-architects`)
5. Dataset은 `production` (기본값) 그대로 둠
6. **Project ID** 와 **Dataset 이름**을 메모해두세요 (다음 단계에서 사용)

> Project ID는 12자리 영숫자 형태입니다. 예: `abc123de`

---

## 2. 환경변수 설정

프로젝트 루트에 `.env.local` 파일을 만들고 아래 내용을 채웁니다:

```
NEXT_PUBLIC_SANITY_PROJECT_ID=여기에_PROJECT_ID_입력
NEXT_PUBLIC_SANITY_DATASET=production
```

`.env.local.example` 파일을 복사해서 사용하시면 됩니다.

---

## 3. Studio 접속 (관리자 페이지)

로컬 개발 서버 실행:
```bash
npm run dev
```

브라우저에서 http://localhost:3000/studio 접속.

처음 접속하면 Sanity 로그인 화면이 뜹니다. 가입한 계정으로 로그인하면
관리자 페이지(Studio)가 열립니다.

배포 후에는 https://소c-architects사이트.com/studio 로 접속.

---

## 4. 컨텐츠 추가/수정

### 프로젝트 추가
1. Studio 좌측 메뉴에서 **프로젝트** 클릭
2. 우상단 `+` 버튼 → 새 프로젝트 생성
3. 입력 필드:
   - **노출 순서**: 그리드에서 위로 갈수록 작은 숫자 (1, 2, 3…)
   - **위계**: Feature(한 줄 풀폭) / Small(3개 한 줄)
   - **URL 식별자**: 영문 slug (예: `mangmi-ri`) — 한 번 정하면 변경 X
   - **프로젝트명** (한국어/일본어/영어)
   - **위치** (한국어/일본어/영어)
   - **연도, 분야, 상태, 면적, 협업자**
   - **대표 사진** (메인 그리드에 표시될 이미지 1장) — 드래그&드롭 업로드
   - **프로젝트 사진들** (디테일 페이지 갤러리) — 여러 장 한번에 드래그&드롭 가능
   - **설명문** (선택, 한국어/일본어/영어)
4. 우하단 **Publish** 버튼 누르면 사이트에 즉시 반영

### 사진 변경
1. 해당 프로젝트 열기 → **대표 사진** 클릭 → 새 사진으로 교체
2. Publish

### 사무소 정보 수정
- Studio 좌측 메뉴 **사무소 정보** → 주소·연락처·소개글 수정 → Publish

---

## 5. 사진 자동 최적화

Sanity Studio에 사진을 업로드하면:
- Sanity CDN에 자동 저장
- 사이트에서 디바이스 크기에 맞는 적절한 해상도로 자동 변환되어 노출
- 별도의 리사이즈 작업 불필요

---

## 6. 무료 플랜 한도

- 사용자 3명까지 무료
- 월 100,000개 API 요청
- 5GB 사진 저장
- 일반적인 건축사무소 포트폴리오에는 충분

한도를 넘기면 알림이 오고, 그때 결정해서 유료 플랜으로 전환 가능.

---

## 7. 문의

- Sanity 사용 중 어려움이 있으시면 홍준스튜디오로 연락 부탁드립니다
- CMS 사용법 동영상 가이드가 필요하시면 별도 제작 가능

© so.c_architects · 홍준스튜디오
